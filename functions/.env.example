# Copy to .env.<firebase-project-id>; never commit the project-specific copy.
# Phase 8 staging enables these only after its accounting baseline is ready.
PADELX_RATING_CONTRIBUTIONS_READY=false
PADELX_ACCOUNT_DELETION_ENABLED=false
